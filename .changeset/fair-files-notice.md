---
"oc-usage-limits-plugin": patch
---

Stop publishing OpenCode runtime packages as peer dependencies and keep them as dev-only build dependencies. Bump the OpenTUI/Solid build packages to current releases and pin patched Seroval resolution to avoid install conflicts and audit failures.
