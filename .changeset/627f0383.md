---
"oc-usage-limits-plugin": minor
---

Add Synthetic and MiniMax Token Plan providers. Both surface 5h rolling and weekly quota windows in the sidebar; credentials are looked up from `apiKey` config (with `{env:...}` support), an optional `authPath` JSON file, or OpenCode's shared `auth.json`.
