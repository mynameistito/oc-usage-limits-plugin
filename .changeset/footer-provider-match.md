---
"oc-usage-limits-plugin": patch
---

Footer now falls back to the first enabled provider when the active session's provider is disabled or not found in the plugin registry, instead of rendering nothing.
